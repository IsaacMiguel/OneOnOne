import { Button, } from "./Button"

export default {
  component: Button,
}

export const Primary = {
  name: "basic button",
  args: {
    label: "Click me!",
    isLoading: false,
  },
}
